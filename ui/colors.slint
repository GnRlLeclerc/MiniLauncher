// Global color definitions

// Color scheme
export global LauncherColors {
    in-out property <color> background;
    in-out property <color> text;
    in-out property <color> border;
    in-out property <color> accent;
    in-out property <color> highlight;
}
